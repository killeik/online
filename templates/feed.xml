<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
  <title>{{ config.title }}</title>
  <subtitle>{{ config.description }}</subtitle>
  <link href="{{ feed_url | safe }}" rel="self" />
  <link href="{{ config.base_url | safe }}" />
  <updated>{{ last_updated | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>
  <id>{{ config.base_url | safe }}</id>
  <author>
    <name>{{ config.extra.author }}</name>
    <email>{{ config.extra.email }}</email>
  </author>
  {% for page in pages %}
    <entry>
      <title>{{ page.title }}</title>
      <link href="{{ page.permalink | safe }}" />
      {% if page.updated %}<updated>{{ page.updated | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>{% elif page.date %}<updated>{{ page.date | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>{% endif %}
      <id>{{ page.permalink | safe }}</id>
      <content type="html">{% if page.extra.link is defined %}&lt;a class=&quot;primary&quot; href=&quot;{{ page.extra.link | escape_html | safe }}&quot;&gt;&lt;h3&gt;{{ page.title | escape_html | safe }}&lt;/h3&gt;&lt;/a&gt;{% endif %}{{ page.content | escape_html | safe }}</content>
    </entry>
  {% endfor %}
</feed>
